# Assumptions

Every guess made in the absence of field data. Walk this list with the pilot vendor at the end of the build, item by item.

Mark the corresponding code with `// ASSUMPTION: ... See docs/assumptions.md#anchor`. Anchors are stable; numbers are for reading (the table is numbered 1–42 in the order the guesses were made, with no gaps — reference anchors in code, never numbers).

## Vendor afternoon checklist

The ten guesses that cost most if wrong, in the order to ask them in one afternoon at the desk — money first (each answer is a setting the desk can change that evening), then the warehouse (each answer is something to *watch*, not ask). Bring the phone with the demo on it and a printed rate card of ours to compare against theirs.

| Ask | # | Anchor | The question, as you would say it | What changes if the answer differs |
|---|---|---|---|---|
| 1 | 5 | `#week-rate` | "Show me your rate card. How do you quote a ten-day job?" | One org setting (`week_equals_days`); the whole quote pipeline reads it. |
| 2 | 21 | `#rental-day` | "What time does gear have to be back to avoid another day? Is there a grace window?" | One expression, maybe one more knob on the card. |
| 3 | 2 | `#deposit-norms` | "What do you take from a stranger before a Rs 4M camera leaves — cash, a cheque, CNIC? What happened last time gear came back damaged?" | The deposit state machine and the refund gate are built around this; the phone door is still to build, so the answer shapes it. |
| 4 | 1 | `#hold-ttl` | "How is a hold placed today? Who calls whom when two clients want the same body, and how long does a hold live?" | `pencil_ttl_hours` per org; the escalation trigger. |
| 5 | 3 | `#cash-split` | "How were the last ten jobs paid?" | Which payment surfaces come first. |
| 6 | 20 | `#seasonal-pricing` | "Is an Eid-week or December quote ever higher than a March one for the same kit? By how much?" | Rows on the calendar (data); whether MAX is the right rule. |
| 7 | 11 | `#tag-survival` | *Do, don't ask:* tag one rack today; look at it after a month of real jobs. | If tags fail often, manual entry becomes the primary path — the design's largest bet. |
| 8 | 9 | `#literacy` | *Watch:* hand the phone to a prep tech with no explanation. | Adoption; the icon/colour/haptic-first scanner is built for this. |
| 9 | 10 | `#shared-device` | "How many phones are there, and who carries them?" | PIN fast-switch vs per-user OTP — the auth model is built for the harder case. |
| 10 | 7 | `#fleet-size` | *Count:* the shelves. | The cursor-pull sync sizing; above ~200MB the sync decision is revisited. |

And one hardware question that is not on the list because it is a *doing*: feed one parchi through their receipt printer (`#thermal-58mm`, #42) and read the QR back with any phone.

| # | Anchor | Assumption | Why we're guessing | How we'd know | Cost if wrong |
|---|---|---|---|---|---|
| 1 | `#hold-ttl` | Soft holds ("pencils") default to a 24h TTL, extendable by the desk, with a challenge call when someone else wants to confirm. **Shipped in 0022**: `create_booking` stamps `pencil_expires_at = now() + 24h`, overridable per call and per org (`settings.pencil_ttl_hours`); expiry is a read predicate plus opportunistic pruning inside the booking RPCs, never a cron. | The research could not find a primary source documenting the pencil/challenge convention on the open web, despite it being central to the booking design. | Ask the vendor how a hold is placed today, who calls whom, and how long it lives. | Medium. The state machine is right; only the TTL and the escalation trigger would change — both are settings, not schema. |
| 2 | `#deposit-norms` | Deposits are collected per booking, in cash or as a held cheque, and are refunded only after check-in reaches `inspected_clear`. | No sourceable data on Pakistani rental-house deposit practice. | Ask what they take, from whom, and what they've done when gear came back damaged. | High. The deposit state machine and the refund gate are built around this. |
| 3 | `#cash-split` | Money is cash-first, with running client credit (*udhaar*) as a real instrument. Card is rare. | Pakistan's 88% digital retail transaction share is formal-rails data and does not evidence that a rental house takes card. | Ask how the last ten jobs were paid. | Medium. Affects which payment surfaces get built first, not the ledger design. |
| 4 | `#coi` | Formal Certificates of Insurance are uncommon here; CNIC + guarantor + held cheque are the trust artefacts instead. `customer_credentials` generalises COI to cover all four. | Research found the COI norm documented only for US rental houses. | Ask what they require before handing over a Rs 4,000,000 camera to someone new. | Low. The credentials table already generalises; only the default kinds change. |
| 5 | `#week-rate` | A week bills as 3 day-rates (`week_equals_days = 3`), configurable per org. | The 3-day-week convention is sourced (MovieMaker) but for US houses. | Ask for their current rate card and how they quote a 10-day job. | High if the default is wrong on day one, but it is one org setting. |
| 6 | `#weekend` | The weekend day-mask is Sat/Sun, with Friday treated as a full day. | Friday Jumma is a real half-day in Pakistani production scheduling, but whether it affects *billing* is unknown. | Ask how they bill a Thursday-to-Saturday job. | Low. The calendar object takes a day-mask; this is data, not code. |
| 7 | `#fleet-size` | A typical Lahore house holds ~500–4,000 taggable assets, sized as an 8–20MB device working set. | No sourceable fleet-size data for this market. | Count their shelves. | High if wildly off — it is the basis for choosing cursor-pull sync over PowerSync. Above ~200MB, revisit. |
| 8 | `#roman-urdu` | Roman Urdu (Latin script) is more usable than Nastaliq for warehouse staff, and avoids bidi problems next to LTR asset codes. | Reasoned from WhatsApp typing habits, not tested with actual warehouse staff. | Show two screens to three techs. | Medium. Changes the font package and every fixed-height row token. |
| 9 | `#literacy` | Prep techs can read short Roman-Urdu labels but the scanner should be operable primarily by icon, colour and haptic. | The research prescribes low-literacy tolerance without quantifying it. | Watch a tech use it without explanation. | High for adoption, low for architecture. |
| 10 | `#shared-device` | Warehouse phones are shared between 2–4 staff, hence PIN-based fast user switching rather than per-user OTP. | Inferred from the business shape and from Current RMS's documented OTP failures. | Ask how many phones exist and who carries them. | Medium. The auth model is built for the harder case, so being wrong is safe. |
| 11 | `#tag-survival` | Printed QR labels survive gaffer tape residue, road cases and Lahore summer heat well enough that re-tagging is occasional, not routine. | Untested. The whole design assumes tags mostly persist. | Tag one rack and check it after a month of real jobs. | High. If tags fail often, the manual fallback stops being a fallback and becomes the primary path. |
| 12 | `#qc-gate` | Check-in reconciliation is **advisory** — it alerts, it never blocks closing a return. | A judgement call. A house might prefer it to block. | Watch one real return at 7pm. | Low. One flag. |
| 13 | `#clock-skew` | The 5-minute threshold for back-correcting device clock drift. | An explicit guess in the architecture document. | Instrument `clock_offset_ms` and look at the real distribution across the first 20 devices. | Low. Ordering is by `server_time`; this only affects display and late-arrival detection. |
| 14 | `#value-threshold` | The "new customer + high value" fraud gate triggers above a configurable rupee threshold. | No data on what counts as high-value here. | Ask what they'd want a second opinion on. | Low. It's an org setting. |
| 15 | `#demo-rates` | The demo's per-product day rates and replacement values (FX9 ~Rs 25,000/day and ~Rs 3.5M replacement, lenses Rs 8–12k/day, an XLR Rs 300), and western digit grouping (Rs 250,000, never lakh-style 2,50,000) on every money label. | No sourceable Lahore rate card; the grouping convention is reasoned from local invoices and bank statements, not verified with a vendor. | Ask for their actual rate card and one paper challan, and copy both. | Low. The rates are demo seed data the real card replaces; the grouping is one formatter (`formatRupees`) in `packages/core`. |
| 16 | `#buffer-defaults` | Prep and turnaround buffers default to 2h before and 4h after the customer window (`settings.prep_buffer_hours` / `turnaround_buffer_hours`, per org; shortenable per booking at confirm — PLAN override 7). | The architecture's illustrative numbers ("a 2-hour prep window and a 4-hour turnaround") promoted to defaults; no field data on real Lahore turnaround practice. | Watch one same-day turnaround: when does gear actually become re-rentable after a 7pm return? | Low. Two org settings and a per-booking override already exist; only the defaults would change. |
| 17 | `#booking-exposure` | The credential gate measures a booking's exposure as the summed **replacement value** of what it promises (`confirm_booking`, 0022 D9). | Rental rates don't exist until phase 3, and replacement value is what actually walks out the door — but a vendor might reason in rental value or deposit size instead. | Ask what number they look at before releasing gear to a stranger. | Low. One SUM expression inside one RPC. |
| 18 | `#least-utilized` | "Least-utilised" allocation picks the unit with the lowest `rental_days_since_service`, then lowest `cycle_count` (0022 D4). | Days-since-service is the only usage meter that exists (0021); true lifetime utilisation isn't tracked yet, and a house might prefer round-robin or newest-first. | Ask which body they'd hand out first and why. | Low. One ORDER BY inside confirm_booking; the constraint re-checks whatever the pick is. |
| 19 | `#weekend-free` | Days in the rate card's `weekend_mask` are **not billed** — but the mask defaults to **empty: every day bills**. A house that gives the weekend away sets `{6,7}` (Sat/Sun per #6, Friday a full day) on its card; then a Fri 10:00 → Mon 10:00 job counts one day and a weekend-only job still bills `min_billable_days`. **Shipped in 0024** (D3): the mask is per card, the trace lists every dropped date. | The "weekend = one day" rule is a sourced US convention; Lahore's highest-value jobs are shaadi weekends, so a default that quietly gave them away would be an under-bill no client ever reports. Opt-in was judged the honest default. | Ask how they bill a Thursday-to-Saturday job, and whether any client is ever excused a Sunday the gear sat idle. | Low. One array per card, no code. |
| 20 | `#seasonal-pricing` | Dec–Feb is the wedding season and it is **shading only**: `ensure_default_calendar` seeds the rows at `rate_multiplier = 1.0`; holidays (Eid) are rows the desk adds with whatever multiplier they use. **Shipped in 0024** (D6/D7): the pipeline applies the MAX multiplier of any calendar day inside the job, booking-wide. | Vendor-dream-plan flags "is seasonal/peak pricing actually practiced, or is pricing purely relational discounting?" as unanswered. A 1.0 default cannot surcharge anyone by accident. | Ask whether an Eid-week or December quote is ever higher than a March one for the same kit, and by how much. | Low. Edit the rows; nothing in code changes. Whether MAX (vs per-day blend) is the right rule is a follow-up question for the same afternoon. |
| 21 | `#rental-day` | A rental day is **24 hours from pickup**, dated in the org timezone: out Tue 10:00, back Fri 10:00 is three days; back at 10:01 is four. **Shipped in 0024** (D2). | The alternative — every calendar date touched — bills the return morning, which a return-by-the-same-time house would not defend; but a grace period (return by noon = same day) is common and unverified here. | Ask what time gear has to be back to avoid another day, and whether there is a grace window. | Medium. `ceil(duration / 24h)` is one expression; a grace window is one more knob on the card. |
| 22 | `#override-final` | A manual rate override is the **final** per-day rate: the calendar multiplier does not apply on top of it. **Shipped in 0024** (D8). | "Never fight the owner's judgement" (research) — when the owner says Rs 20,000 for this client, that is the number, not Rs 20,000 × 1.25. A house that thinks of overrides as "base rate before season" would disagree. | Show the owner an Eid-week quote with an override and ask what the line should read. | Low. One `case` in `price_booking` step 5; the trace already reports `multiplier_applied`. |
| 23 | `#sub-hire-cost` | The cost side of margin-before-quote is **every live expense** tagged to the booking (or to the job it became), whatever its kind — transport to set counts as much as the sub-hired lens. **Shipped in 0024** (D9). | The vendor-dream-plan says "sub-rental costs and discounts net out on the quote screen"; whether the house wants transport/consumables in the same number is a guess. | Ask what they subtract before deciding a quote is worth it. | Low. One `where` clause in `booking_sub_hire_cost`; the kinds are already on every row. |
| 24 | `#wedding-season` | December–February is wedding season in Lahore: the calendar shades those months and the desk expects them booked 4–6 months out (`seasonFor` in `packages/core/src/bookings.ts`; vendor-dream-plan Phase C1). | The plan's own line ("Dec–Feb is booked 4–6 months out") is the only source; no vendor has confirmed the months or whether Ramadan/Eid shifts them year to year. | Ask which months they refuse pencils for and start asking for deposits early; look at last year's calendar. | Low. One pure function of the month; the shading is cosmetic and the booking rules do not branch on it. |
| 25 | `#escalation-ladder` | The overdue ladder is day 1 WhatsApp nudge → day 3 call → day 7 late-fee draft → day 14 manager escalation with a blacklist consideration (`escalationStep` in `packages/core/src/bookings.ts`). PLAN override 19 sketches "1 day → tech, 3 days → manager WhatsApp, 14 days → a missing claim"; this is the desk-facing reading, with the day-7 money rung the khata already drafts. | No field data on when a Lahore house stops nudging and starts calling, or when it writes a client off. | Ask what they did on the last three late returns, day by day. | Low. Four rungs in one array; each carries an action kind a screen maps to one button, so re-ordering is data. |
| 26 | `#local-credential-flag` | On the phone, the confirm gate reads one local flag (`customers.credentials_verified`) in place of the server's derived `verified_customers` view, and the refusal says so (`gate: 'local_flag'` in `confirmBooking`, `apps/app/src/demo/bookings.ts`). | The phone carries no credential rows (0009/0015 keep CNICs off it), so "verified" cannot be derived locally; the server re-runs its own view when the op replays. | Watch whether a desk ever confirms offline for a stranger the server later refuses — that count is the cost of the proxy. | Low. The server's gate is the law; the flag only decides what the desk sees before the pipe drains. |
| 27 | `#sub-rent-intent` | On the extension screen, the **Sub-rent** door records an intent — a note on the extending booking and a `sub_rent_intent` outbox op — and the extension is then written locally with that collision acknowledged, chained BEHIND the intent op so the server replays the sub-rent (W7's partner network) before the extend (`noteSubRent` / `ExtendOptions.acknowledged` in `apps/app/src/demo/bookings.ts`). The other client's claim on the unit stays until a real unit covers it. | 0022's `extend_booking` has no override, and the exclusion constraint cannot hold two confirmed claims on one unit — so "we'll sub-rent for them" can only be an intent until the partner unit exists as an asset row. Whether a desk wants the extension written before the sub-rent is in hand is a field question. | Watch whether an acknowledged extension ever replays before its sub-rent lands, and whether the desk expected the calendar to show the double claim. | Low. One option on one function; the op name is the hook W7 wires. |
| 28 | `#manager-flag` | The overdue ladder's day-14 rung writes one local flag (`job_meta.manager_flagged_at`, `flagForManager` in `apps/app/src/demo/read-model.ts`) and drafts a WhatsApp text to the owner; nothing syncs and no alert row exists for it yet. | The ladder is desk-facing (ASSUMPTION #25); the server has no escalation table, and building one before a house says what "escalate" means to them would be inventing the workflow. | Ask who actually gets told on day 14 today, and by what channel. | Low. A demo-side column beside `departs_at`; a server alert row replaces it when the workflow is known. |
| 29 | `#attendant-custom` | Crew goes out with the gear: a job can carry named **attendants** and a **driver** (`job_attendants`, roles `attendant \| driver`; `assign_attendant` / `unassign_attendant`, 0025 D7), and their display names ride the jobs sync projection (`attendant_names`) so the phone's job card shows who is with the kit. | Crew-out is a high-end custom in the research (a house sends a tech with a Rs 4M body) and is unverified for a Lahore house at all — many send nobody, some send a driver only. | Ask who, if anyone, left with the truck on the last ten jobs, and whether the desk ever needs to know. | Low. One small table and two RPCs; if nobody uses it the column is an empty array on every job. |
| 30 | `#public-tag-url` | The public tag page lives at `orgs.settings.public_tag_url_base` + tag code (`stolen_broadcast_text`, 0025 D9); **no default** — unset means the broadcast carries no link. | No domain or page pattern has been decided for the public resolver (0004/0020 `resolve_tag_public` is the data source, not a page); inventing a URL in SQL would bake a hosting decision into a migration. | Decide the public host with the hosting decision; then set the one setting per org. | Low. One org setting; the text simply gains the link. |
| 31 | `#deposit-hint` | The quote screen's verified-client stamp maps to a four-rung deposit hint: `refuse` (blacklisted) → `lighter` (fast lane: verified + a clean completed rental) → `standard` (verified only) → `full` (stranger) (`customer_quote_flags`, 0025 D10). What "lighter" means in rupees is NOT decided here. | Vendor-dream-plan E5 says a verified client earns a lighter deposit; nobody has said whether that is a percentage, a fixed cut, or the owner's mood on the day. | Ask what deposit a first-timer and a ten-job regular each pay for the same FX9 kit. | Low. Four words in one `case`; the screen renders whatever the hint says and the desk types the number. |
| 32 | `#sub-hire-unpriced` | A sub-hire's agreed cost (in) or charge (out) is **nullable**: a null writes no expense / ledger row and the sub-hire row is the counted, unpriced fact (0025 D6). The desk records the number later through the ordinary money doors. | "Settle it at month end" between partner houses is plausible but unverified; a forced number would be a made-up zero, which the money-honesty rule forbids. | Ask whether partner borrowing is priced per job or netted monthly. | Low. Two nullable columns; if every sub-hire has a number on day one, nothing changes. |
| 33 | `#partner-is-customer` | A partner house that **borrows** from us is a `customers` row with the same name, flagged `is_partner = true` — found by case-insensitive name match or created (`ensure_partner_customer`, 0025 D3). Its balance is `customer_balances` like any client's. | One udhaar book is the design (0017 D1); whether a house keeps a separate mental ledger for partners, and whether "Kamran Rentals" the client and "Kamran Rentals" the partner are the same book to them, is unverified. | Ask whether money owed by a partner house sits on the same khata as client money. | Low. The flag is one column; a separate ledger would be a real change, but nothing today needs it. |
| 34 | `#returned-to-owner` | Retiring a **borrowed** unit (`ownership = 'sub_rented_in'`) stamps `disposition = 'returned_to_owner'`, not `retired` — the reducer derives it from ownership on the existing `retire` verb (0025 D5); `close_sub_hire` mints that event. | The Gone filter must not read "we scrapped it" and "we gave it back" the same; whether the house wants returned units to stay visible in history at all (vs. deleted) is a guess. | Ask what they want to see six months later about a lens they borrowed once. | Low. One `case` in the reducer and one CHECK value; the client mirror (`dispositionFor`) needs the same one-line rule. |
| 35 | `#verified-deposit-hint` | The quote's verified-client stamp reads "VERIFIED — lighter deposit", and the WhatsApp quote's deposit line renders the 0025 #deposit-hint ladder as words: `lighter` = **"half deposit"**, `standard` = "standard deposit", `full` = "full deposit — first booking", `refuse` = "no booking — blacklisted" (`quoteDepositHint` in `apps/app/src/strings.ts`). | Vendor-dream-plan E5 says a verified client earns a lighter deposit; "half" is the first number a house would say, and nobody has said it. | Ask what a ten-job regular actually pays down against the same FX9 kit, and whether it is a fraction or a fixed cut. | Low. Four strings in one lookup; the ladder itself is 0025's. |
| 36 | `#quote-timezone` | The phone prices in **Asia/Karachi** (`DEFAULT_TIMEZONE`, `packages/core/src/pricing.ts`): the calendar dates a rental day falls on — what the weekend mask and the holiday rows are read against — are computed with Intl in that zone, not the phone's. `orgs.timezone` is not mirrored yet, so the org's zone is a constant until a setting carries it. | Every pilot house is in Lahore; a phone that travels (a tech's holiday) must still date a Lahore booking in Lahore. | Ship the org timezone in a projection when a second city exists. | Low. One constant, one parameter every quote already takes. |
| 37 | `#local-quote-flags` | The phone's verified-client stamp (`quoteFlags`, `apps/app/src/demo/khata.ts`) mirrors 0025's `customer_quote_flags` from LOCAL data: `verified` is the one local flag the confirm gate already reads (#26 `#local-credential-flag`), and a job is **clean** when it is closed and its ledger lines carrying its id (charges and payments, deposits aside) net to nothing owed; `fast_lane` and the `deposit_hint` ladder follow 0025's rule over those. | The server derives "clean" from confirmed return dispatches and `job_money_shortfalls`; the phone holds neither the dispatch rows nor the credential rows. A closed, paid-off job is the honest local proxy. | Compare the stamp on the phone with the server's flag for the first twenty quotes; a disagreement is a job closed without its money, which the khata already shows. | Low. The server's view is the law when the quote replays; the flag only decides what the desk sees before the pipe drains. |
| 38 | `#demo-eid` | The demo seeds **one** Eid holiday at 1.25× on the coming **10 April** (`seedRateCard`, `apps/app/src/demo/seed.ts`), so a quote crossing it shows the multiplier line with a real number the moment the demo opens. | Eid moves on the lunar calendar and no seed can know where it lands; the date is a placeholder the desk edits under Settings → Rates, and 1.25 is a round number, not a sourced surcharge. | Ask the house whether Eid week is priced up at all, and by how much (the same afternoon as #20). | Low. One seed row; the real calendar is data the desk types. |
| 39 | `#staff-roster` | The crew picker's roster on the phone is a demo-side `staff` table (three names seeded: Usman, Danish, Saqib — `seedNetwork` in `apps/app/src/demo/network.ts`); the server has users + memberships (0001/0016) but nothing syncs them to a device yet (pre-auth), so `assign_attendant`'s "active member" check is the server's alone. | No device carries a user list before login exists; inventing a users mirror ahead of the auth wave would be a second identity model. | When login lands, replace the table with the org's membership list and watch whether a house's crew is even the same people as its app users. | Low. One small table the auth wave replaces; the ops and the `attendant_names` projection are unchanged. |
| 40 | `#local-asset-code` | A borrowed unit recorded with a serial gets a code minted ON THE PHONE — the product's existing prefix and the next free number (`FX9-04`; a product with no units names itself from its first word) — `nextAssetCode` in `apps/app/src/demo/network.ts`. The server's `generate_asset_code` mints the real one when `record_sub_hire_in` replays, and the pull overwrites the local code. | The label may be printed before the pipe drains, and a unit with no code cannot be scanned or counted; a placeholder that reads like its siblings is the least surprising stand-in. | Watch whether a desk ever prints a label from the local code and then sees it change after sync. | Low. If the server's code differs, one label is reprinted; the unit's id (the QR payload) never changes. |
| 41 | `#ask-market-window` | An "Ask the market" opened from a pasted kit list uses the reply card's dates when the desk has set them, and names **today → tomorrow** as the window in the WhatsApp ask while they are blank or backwards (`askMarket` in `apps/app/src/demo/EnquiryScreen.tsx`); the confirm sheet and the extension screen pass the real booking window. The desk corrects the dates in the sub-hire sheet when a house says yes. | A message with no dates is not an ask a partner can answer; a message with invented dates is a lie. Today-tomorrow is the honest reading of "the client is on WhatsApp right now". | Ask whether the desk types the dates into the reply first, or asks the market first and pins the dates later. | Low. One default in one function; a date picker on the ask sheet is a small follow-up. |
| 42 | `#thermal-58mm` | The thermal parchi targets **58mm paper at 32 columns**, QR model 2 at module size 6, error level M, `GS V 66 0` feed-and-partial-cut, ASCII only (typography folded, LF kept for the QR payload) — `buildParchiEscPos` in `packages/core/src/escpos.ts`. **Hardware unverified**: the bytes are golden-tested against the Epson ESC/POS reference, but no printer has fed paper; the Bluetooth SPP transport is the Capacitor wave (`apps/app/src/print/thermal.ts` is the seam). | The Rs 3,000 Bluetooth receipt printer beside every Lahore till is a 58mm ESC/POS clone by overwhelming odds; 80mm (48 columns) is one option away. | Feed paper: print one parchi on the pilot house's printer and read the QR back with a phone. | Medium if the clone ignores `GS ( k` (some print no QR): the text half still prints and the on-screen QR remains; the fix is a raster-image fallback in the same module. |
| 43 | `#server-name-wins` | The phone adopts the server's id **the moment the server accepts** an op that minted one (`create_booking`, `convert_booking_to_job`, the sub-hires, the rate card, a calendar day): `id_map` records the pair and every local row is re-keyed in the ack's transaction (`rekeyLocal`, `packages/core/src/dispatch.ts`); child rows the phone guessed (prefixed `bl-`/`ar-`/`sr-`/`rce-`) are replaced by the server's on the next pull. | The alternative — keep the phone's name and translate forever — leaves two names for one booking in every query and every URL. One rename at one moment is simpler to reason about; the cost is a page open on the old id going stale for one navigation. | Watch whether a desk ever has a booking page open at the exact moment its op lands, and whether the momentary "no lines" between the ack and the re-pull is ever seen on screen. | Low. The re-key list is data (`REKEY_COLUMNS` + the store's own); a stale page reloads by tapping the row again. |
| 44 | `#pin-echo` | Offline, the PIN gate accepts a PIN whose **echo** — `sha256(device : user : pin)`, kept after every server-verified switch (`packages/core/src/auth.ts`) — matches; a person whose PIN was never verified on this phone cannot pick it up offline. The PIN typed at enrolment is echoed too, though the server only sets it when none existed. Advisory by design: the server stamps every batch with the session's user when it arrives. | A warehouse phone offline for days must still change hands at 6am; asking the server is impossible and asking nothing is a wall or a lie. | Count how often the gate answers from the echo versus the server in the first month, and whether anyone is refused offline for never having been verified. | Low. A wrong echo mislabels nothing the server accepts; if the vendor wants a stricter offline gate, the echo can require a recent server verification. |
| 45 | `#sync-poll-interval` | The loop polls every **30 seconds** while the app is open, plus a cycle on `online`, on coming to the foreground, and on every write that kicks it (`packages/core/src/engine.ts`). | The watermark early-out makes a caught-up poll one indexed read (0.09ms measured), so the cost is the phone's radio, not the server; 30s keeps two desks' calendars within a coffee sip of each other without draining a battery. | Measure battery on the pilot phones and how stale the second desk's calendar ever reads. | Low. One number. |
| 46 | `#enrol-clears-demo` | Enrolling from the demo **clears the pretend house** — mirrors, the demo's own tables, and the demo outbox with its queued demo scans (`DemoStore.goLive`, `apps/app/src/demo/store.ts`); the Enrol screen says so before the button. In live mode re-enrolment keeps everything. | Demo scans are of pretend gear the server has never heard of; replaying them at a real house would only park as cards, and a phone half full of Lahore-demo cameras beside a real fleet is worse than an empty one. | Ask whether anyone expected their demo bookings to become real ones. | Low. Demo data is regenerated on every open. |
| 47 | `#members-mirror` | Every phone in the org carries the **members mirror** (0027): user id, display name, role and whether a PIN is set — drivers included, so the crew picker can name them — and nothing else; a suspended membership leaves the mirror as a tombstone. In live mode this list IS the crew roster (`staff`), copied after every sync (`DemoStore.afterSync`), which resolves #39. | Names and roles are not in the sensitive-column registry (phones and CNICs are), and the PIN gate cannot ask the server for a list while offline. | Ask the house whether a departed employee's name lingering on a phone until its next sync is acceptable, and whether drivers should be pickable at all. | Low. The projection is one block in `pull_changes`; hiding a role is one `where`. |
