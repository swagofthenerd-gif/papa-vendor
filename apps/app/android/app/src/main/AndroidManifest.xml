<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <!-- ML Kit model hint. startScan() links the bundled barcode model,
             so scanning works offline on the loading dock regardless of
             whether Play services ever downloads anything. -->
        <meta-data
            android:name="com.google.mlkit.vision.DEPENDENCIES"
            android:value="barcode_ui" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- The native ML Kit scan path (@capacitor-mlkit/barcode-scanning). -->
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- The feedback vocabulary in packages/core/src/feedback.ts — both the
         Haptics plugin and the navigator.vibrate fallback need this. -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- The thermal parchi over Bluetooth classic SPP (W12,
         PapaPrintPlugin). Android 12 split these into runtime permissions;
         below that the install-time pair covers the same ground, hence the
         maxSdkVersion on the legacy two. Both are requested AT USE, never at
         launch — see PapaPrintPlugin for why.

         neverForLocation on BLUETOOTH_SCAN is a promise the app keeps: it
         talks to a paired till printer and derives nothing about where
         anybody is, so the location permission this would otherwise drag in
         is declined by declaration. -->
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission
        android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation" />
    <uses-permission
        android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30" />
    <uses-permission
        android:name="android.permission.BLUETOOTH_ADMIN"
        android:maxSdkVersion="30" />
    <!-- A phone with no Bluetooth radio still runs everything else; the
         printer row says "no printer" and the parchi stays on screen. -->
    <uses-feature android:name="android.hardware.bluetooth" android:required="false" />
</manifest>
